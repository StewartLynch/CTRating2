import XCTest
@testable import CTRating2

final class CTRating2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CTRating2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
